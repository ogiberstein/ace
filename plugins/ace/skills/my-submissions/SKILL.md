---
name: my-submissions
description: Show the logged-in user's ACE submission queue statuses on demand. Not injected into default sessions.
---

# /ace:my-submissions

Call `ace_submissions({})` and summarize each submission id, title, status, verdict/reject reason if present, and published capsule id if available.
